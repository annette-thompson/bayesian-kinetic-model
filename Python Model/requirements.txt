# The Alpine environment that runs this project's cluster fits, read on 2026-09-25 by importing
# each package, except pymc 6.1.0 in place of Alpine's 6.0.1: pymc 6.0.1 declares pytensor < 3.1,
# so it cannot be installed alongside pytensor 3.1.2. environment.yml (conda-forge) carries the
# same pins and is the reference for a fresh build; these are for a pip install into an existing
# Python 3.12 env:
#
#   pip install -r requirements.txt
#
# jax 0.7.0 pairs with equinox 0.13.0, diffrax 0.7.1 and optimistix 0.0.10. Later equinox (0.13.x)
# and optimistix (0.1.0) releases declare jax 0.7.0 and 0.7.1 incompatible.
#
# GPU (Linux only): the CUDA plugin must match jax exactly (jax == jaxlib == jax-cuda12-plugin ==
# jax-cuda12-pjrt). The markers keep this file installable on macOS, where there is no CUDA wheel.
#
# Python 3.12.

# --- sampling / probabilistic stack ---
pymc==6.1.0
pytensor==3.1.2
blackjax==1.3
numpyro==0.21.0
nutpie==0.16.11
arviz==1.2.0
arviz-base==1.2.0
preliz==0.27.1

# --- ODE solving and JAX ---
jax==0.7.0
jaxlib==0.7.0
jax-cuda12-plugin[with-cuda]==0.7.0; sys_platform == "linux"
jax-cuda12-pjrt==0.7.0; sys_platform == "linux"
diffrax==0.7.1
equinox==0.13.0
lineax==0.1.0
optimistix==0.0.10
jaxopt==0.8.3
jaxtyping==0.3.11

# --- numerics / IO ---
numpy==2.0.0
scipy==1.18.0
zarr==3.2.1
netCDF4==1.7.4
numba==0.65.1
llvmlite==0.47.0
xarray==2026.4.0

# --- imported directly by Utilities/ and Calculation Files/ ---
sympy==1.14.0
seaborn==0.13.2
matplotlib==3.11.0
pandas==3.0.3
PyYAML==6.0.3
platformdirs==4.10.0

# --- notebooks ---
ipykernel==7.3.0
ipython==9.15.0
