# Exact versions from the two environments that are known to run this project,
# captured 2026-08-12. Prefer environment.yml (conda-forge) for a fresh build --
# these pins exist so a result can be traced to a stack, and so a divergence
# between the two machines is visible rather than guessed at.
#
#   pip install -r requirements.txt      # into an existing python 3.12 env
#
# The two stacks are NOT the same, and both produce finite, agreeing gradients at
# rtol 1e-4 / atol 1e-10. Timings are only comparable within a machine.
#
# GPU note: on linux-64 the jax CUDA plugin must match jax exactly
# (jax == jaxlib == jax-cuda12-plugin == jax-cuda12-pjrt). Mismatched versions
# fail at import with "cannot import name 'make_single_device_sharding'".
#
# ---------------------------------------------------------------------------
#                                nate (RTX 3080)   Alpine (A100)
# python                         3.12.13           3.12.13
# jax / jaxlib                   0.10.2            0.7.0
# pymc                           6.2.0             6.0.1
# pytensor                       3.2.4             3.1.2
# diffrax                        0.7.2             0.7.1
# equinox                        0.13.8            0.13.0
# blackjax                       1.6.2             1.3
# numpy                          2.4.6             2.0.0
# zarr                           3.3.0             3.2.1
# matplotlib                     3.11.1            3.10.9
# pandas                         3.0.5             3.0.3
# ---------------------------------------------------------------------------

# ---- nate (RTX 3080 workstation) -- newer stack, verified 2026-08-12 ----
jax==0.10.2
jaxlib==0.10.2
# CUDA plugins are linux-only wheels; the marker keeps this file installable on
# macOS (where the local repo lives) instead of failing the whole install.
jax-cuda12-plugin==0.10.2; sys_platform == "linux"
jax-cuda12-pjrt==0.10.2; sys_platform == "linux"
pymc==6.2.0
pytensor==3.2.4
diffrax==0.7.2
equinox==0.13.8
blackjax==1.6.2
numpyro==0.21.0
nutpie==0.16.11
arviz==1.2.0
arviz-base==1.2.0
preliz==0.27.1
numpy==2.4.6
scipy==1.18.0
zarr==3.3.0
# required to WRITE the posterior netcdf; without it inference finalization
# raises at the end of an otherwise successful run
netCDF4==1.7.2
sympy==1.14.0
seaborn==0.13.2
matplotlib==3.11.1
pandas==3.0.5
PyYAML==6.0.3
numba==0.65.1
llvmlite==0.47.0
jaxtyping==0.3.11
platformdirs==4.11.2
ipykernel==7.3.0

# ---- Alpine (A100 cluster) -- older stack, produced the earlier results ----
# Swap these in wholesale if reproducing a pre-2026-08 result; do not mix with
# the block above.
# jax==0.7.0
# jaxlib==0.7.0
# pymc==6.0.1
# pytensor==3.1.2
# diffrax==0.7.1
# equinox==0.13.0
# blackjax==1.3
# numpy==2.0.0
# zarr==3.2.1
# matplotlib==3.10.9
# pandas==3.0.3
