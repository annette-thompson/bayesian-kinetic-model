# Build output of the retired Sync/make_utils32.sh (a 32-bit copy of Utilities/).
# The script is gone, but old copies of the tree may still sit in working copies.
Utilities32/

# Resumable-sampler checkpoints (zarr draw stores plus resume state). They belong with
# the run that wrote them, and one run's store is hundreds of chunk files.
Results/**/checkpoint/
