# Loose pins: the same package set as requirements.txt, but with only the bounds
# that are actually load-bearing. Use this when you want a current stack rather
# than a reproduction of a specific run -- e.g. building on a new machine, or
# letting the solver pick versions that suit its CUDA.
#
#   pip install -r flexible_requirements.txt
#
# Both stacks this project runs on (jax 0.7.0 / pymc 6.0.1 on Alpine, jax 0.10.2 /
# pymc 6.2.0 on nate) satisfy these bounds and both produce correct gradients, so
# the range below is exercised rather than assumed.

# --- sampling / probabilistic stack ---
pymc>=6.0
pytensor>=3.1
blackjax>=1.3
numpyro>=0.21
nutpie>=0.16
arviz>=1.2
arviz-base>=1.2
preliz>=0.27

# --- ODE solving and JAX ---
# jax and jaxlib must be the SAME version, and on linux the CUDA plugin/pjrt must
# match both, or jax fails at import.
jax>=0.7
jaxlib>=0.7
jax-cuda12-plugin>=0.7; sys_platform == "linux"
jax-cuda12-pjrt>=0.7; sys_platform == "linux"
diffrax>=0.7
equinox>=0.13
jaxtyping>=0.3

# --- numerics / IO ---
numpy>=2.0
scipy>=1.18
zarr>=3            # hard requirement: the checkpoint store uses the zarr 3 API
# required to WRITE the posterior netcdf; without a backend, finalization
# raises at the very end of an otherwise successful multi-hour run
netCDF4>=1.7
numba==0.65.1      # hard pin: newer numba wants an llvmlite pytensor rejects
llvmlite>=0.47
platformdirs>=4

# --- imported directly by Utilities/ and Calculation Files/ ---
# Listed explicitly because nothing above pulls sympy or seaborn in; leaving them
# implicit is what broke the first real run on nate.
sympy>=1.14
seaborn>=0.13
matplotlib>=3.10
pandas>=3.0
PyYAML>=6.0

# --- notebooks ---
ipykernel>=7.0
